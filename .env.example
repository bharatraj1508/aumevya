# MongoDB Atlas connection string (Phase 1 CMS + inquiries)
DATABASE_URL=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/aumevya?retryWrites=true&w=majority

# Payload encryption/JWT secret — generate with: openssl rand -hex 32
PAYLOAD_SECRET=

# Public site URL (used for admin + email links)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# --- Media storage (wired in a follow-up step) ---
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# --- Email (Gmail SMTP via Payload) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USER=
# SMTP_PASS=            # Gmail App Password (not your account password)
# EMAIL_FROM=
# ADMIN_NOTIFY_EMAIL=
